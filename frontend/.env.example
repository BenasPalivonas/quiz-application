# Copy to .env.local and fill in real values.
# Loaded by both apps/web and apps/e2e via dotenv-cli (see their package.json scripts).

# Backend API base URL. Used by apps/web and by apps/e2e's Playwright tests.
API_URL=http://127.0.0.1:8000/api
